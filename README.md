# Movie Reviews Website 🎬

Мини-проект на Django: сайт с обзорами фильмов, рейтингами, комментариями и личными профилями.  
Фронт: Django templates + ванильный JS + CSS. (Проект на этапе MVP, планируется расширение функционала и рефакторинг)

---

## Возможности

📝 Добавление фильмов (через админку)  
⭐ Рейтинги фильмов  
💬 Комментарии к фильмам (возможность отвечать на комментарии)  
👤 Профили пользователей (аватар, bio, список оценённых фильмов)  
📊 Статистика: средний рейтинг, количество комментариев  
🔎 Поиск фильмов
---

## Технологии

- Python / Django (проект: `movie_reviews_website_project`, основное приложение: `core`)  
- База данных: MS SQL Server (через `django-mssql-backend`)  
- Статика: `core/static/` → сборка в `staticfiles/`  
- Медиа: `media/` (аватары, изображения фильмов)  

---

## Структура проекта
movie_reviews_website_project/
├─ manage.py # Точка входа Django
├─ movie_reviews_website_project/ # Настройки проекта (settings, URLs, WSGI)
├─ core/ # Основное приложение (models, views, templates, static)
├─ media/ # Пользовательские загрузки (не коммитится)
├─ staticfiles/ # Результат collectstatic
├─ venv/ # Виртуальное окружение
└─ .env # Конфигурация окружения
## Быстрый старт (локально)

1️⃣ Клонировать репозиторий  

```bash
git clone https://github.com/your-username/movie_reviews_website_project.git
cd movie_reviews_website_project
2️⃣ Создать и активировать виртуальное окружение

python -m venv venv
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate


3️⃣ Установить зависимости

pip install -r requirements.txt


4️⃣ Создать базу данных в MS SQL Server (если ещё нет):

CREATE DATABASE movie_reviews_db;


5️⃣ Настроить .env:

DB_ENGINE=mssql
DB_NAME=MovieReviewsDB
DB_USER=MONSIER\kolyb
DB_PASSWORD=
DB_HOST=MONSIER\SQLEXPRESS
DB_PORT=1433
SECRET_KEY=your_django_secret_key
DEBUG=True


Если используете Windows Authentication, DB_PASSWORD можно оставить пустым.

6️⃣ Применить миграции

python manage.py migrate


7️⃣ Запустить сервер

python manage.py runserver


8️⃣ Открыть в браузере

http://127.0.0.1:8000/